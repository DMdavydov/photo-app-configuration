login.url.path: "/users/login"
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/photo_app?serverTimezone=UTC
    username: robik
    password: kibor
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest